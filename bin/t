#! /usr/bin/env ruby
require 'optparse'
require '../lib/t'

puts ARGV.count
puts ARGV[0]

t = T.new

if ARGV.count.equal? 0
  t.listTasks
elsif ARGV.count.equal? 1
  t.clearList if ARGV[0].equal? "-c"
elsif ARGV.count.equal? 2
  puts "TODO delete a task" if ARGV[0].equal? "-d"
end
